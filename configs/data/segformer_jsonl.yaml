defaults:
  - _self_

_target_: src.data.local_seg.SegJsonDataModule
train_jsonl: ???
val_jsonl: ???
size: ${size}
resize_mode: ${resize_mode}
batch_size: 4
val_batch_size: 1
workers: 4
val_workers: 1
# classid (default): raw class-id Long map for SegIDStructureMapperXL's
# embedding stem. rgb: legacy palette-colorized map for FixedStructureMapperXL.
# Must match configs/lora/mapper_network/{segidXL,fsmXL}.
conditioning: classid
