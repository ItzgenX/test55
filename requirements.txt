# Pinned to the exact versions this repo has actually been verified against
# (local dev conda env "loradapter": real training runs, checkpoints, and
# inference all executed on this stack -- see CLAUDE.md Status section).
#
# torch/torchvision are built for CUDA 13.0 (+cu130) on the dev box -- pick
# the build matching YOUR cluster's CUDA driver instead of copying +cu130
# blindly (e.g. install from the matching index: https://download.pytorch.org/whl/<cuXXX>).
# The version numbers (2.11.0 / 0.26.0) should still match.
torch==2.11.0
torchvision==0.26.0

diffusers==0.37.1
transformers==5.5.0
huggingface-hub==1.9.2
tokenizers==0.22.2
safetensors==0.7.0
accelerate==1.13.0

hydra-core==1.3.2
jaxtyping==0.3.9
einops==0.8.2
numpy==2.4.4
pillow==12.2.0
tqdm==4.67.3
tensorboard==2.20.0

open-clip-torch==3.3.0
torch-fidelity==0.4.0
basicsr==1.4.2
